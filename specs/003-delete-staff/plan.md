# Implementation Plan: Supprimer les membres du personnel

**Branch**: `003-delete-staff` | **Date**: 2026-02-06 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/003-delete-staff/spec.md`

## Summary

Implémenter la fonctionnalité de suppression des membres du personnel avec confirmation préalable. La suppression sera définitive (hard delete) et nécessitera la création d'un composant de dialogue de confirmation réutilisable. Le service `deleteStaffMember` existe déjà.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode)
**Primary Dependencies**: SolidJS 1.9, @supabase/supabase-js
**Storage**: Supabase PostgreSQL avec RLS
**Testing**: Vitest + @solidjs/testing-library
**Target Platform**: Web (navigateurs modernes)
**Project Type**: SPA (Single Page Application)
**Performance Goals**: Suppression en < 1 seconde, feedback immédiat
**Constraints**: Confirmation obligatoire avant suppression, accessibilité clavier
**Scale/Scope**: Single-user app, gestion de 1-50 membres du personnel

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Type-Safe First | ✅ PASS | TypeScript strict, interfaces définies dans contracts/ |
| II. Test-Driven Development | ✅ PASS | Tests prévus avant implémentation |
| III. Ship Fast, Iterate Often | ✅ PASS | Feature simple, réutilise code existant |
| IV. Component-Based Architecture | ✅ PASS | ConfirmDialog comme composant réutilisable |
| V. Supabase-Native Patterns | ✅ PASS | deleteStaffMember utilise supabase client, RLS en place |
| VI. Explicit Error Handling | ✅ PASS | Try-catch, showError pour les erreurs |
| VII. Accessibility by Default | ✅ PASS | Focus trap, keyboard nav, aria-attributes |

**Post-Design Re-check**: Tous les principes respectés. Aucune violation.

## Project Structure

### Documentation (this feature)

```text
specs/003-delete-staff/
├── plan.md              # This file
├── research.md          # Technical decisions
├── data-model.md        # Entity documentation
├── quickstart.md        # Testing guide
├── contracts/           # API contracts
│   ├── staff-service.ts
│   └── confirm-dialog.ts
└── tasks.md             # Generated by /speckit.tasks
```

### Source Code (repository root)

```text
src/
├── components/
│   ├── ui/
│   │   ├── Toast.tsx           # Existing - notifications
│   │   ├── ConfirmDialog.tsx   # NEW - confirmation modal
│   │   └── ConfirmDialog.test.tsx  # NEW - tests
│   └── staff/
│       ├── StaffList.tsx       # MODIFY - add delete button
│       └── StaffList.test.tsx  # MODIFY - add delete tests
├── services/
│   ├── staff.service.ts        # Existing - deleteStaffMember function
│   └── staff.service.test.ts   # Existing - has delete tests
├── lib/
│   ├── supabase.ts             # Existing
│   └── types/
│       └── database.ts         # Existing - StaffMember type
└── pages/
    └── Staff.tsx               # MODIFY - handle delete callback
```

**Structure Decision**: SolidJS SPA avec composants colocalisés. Le nouveau composant ConfirmDialog sera placé dans `src/components/ui/` pour réutilisabilité.

## Implementation Phases

### Phase 1: ConfirmDialog Component

1. Créer `src/components/ui/ConfirmDialog.tsx`
   - Modal overlay avec backdrop
   - Focus trap pour accessibilité
   - Fermeture sur Escape/clic extérieur
   - Props: isOpen, title, message, onConfirm, onCancel

2. Créer `src/components/ui/ConfirmDialog.test.tsx`
   - Test d'ouverture/fermeture
   - Test des callbacks
   - Test keyboard navigation

### Phase 2: Delete Button in StaffList

1. Modifier `src/components/staff/StaffList.tsx`
   - Ajouter bouton supprimer dans chaque carte
   - State pour membre à supprimer
   - Intégrer ConfirmDialog

2. Mettre à jour `src/components/staff/StaffList.test.tsx`
   - Test bouton visible
   - Test ouverture dialogue
   - Test suppression

### Phase 3: Integration & Polish

1. Modifier `src/pages/Staff.tsx`
   - Callback pour rafraîchir liste après suppression

2. Ajouter CSS pour ConfirmDialog dans `src/App.css`

3. Tests end-to-end manuels avec quickstart.md

## Complexity Tracking

> Aucune violation de la Constitution. Pas de complexité ajoutée.

| Aspect | Complexité | Justification |
|--------|------------|---------------|
| ConfirmDialog | Faible | Composant réutilisable simple |
| Intégration | Faible | Service existant, pattern établi |
